# Secrets (절대 커밋 금지)
static/serviceAccountKey.json
static/upload_results.json

# Optional: 로컬 전용이면 이것도 제외(원하면 주석 해제)
# static/firebase_url_mapping.json

# Static images (만약 여기에 이미지가 있으면)
static/images/

# Python cache
__pycache__/
*.pyc